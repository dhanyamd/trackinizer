# Only the symbol this repo reads: the resolved cache root (HF_HOME via env,
# already defaulted by the library if unset). See __init__.pyi's note -- a new
# import from this package must be added here too.

HF_HOME: str
